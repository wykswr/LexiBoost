version: '3'
services:
  mongodb:
    image: mongo
    container_name: my-mongodb
    ports:
      - 27017:27017
    env_file:
      - backend/.env
  express:
    build: ./backend
    container_name: my-express
    ports:
      - 5000:5000
    depends_on:
      - mongodb
